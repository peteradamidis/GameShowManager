# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# PostgreSQL connection - format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/contestant_db

# Individual database parameters (used by drivers)
PGHOST=localhost
PGPORT=5432
PGUSER=user
PGPASSWORD=password
PGDATABASE=contestant_db

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================
# Session encryption secret - use a random string (minimum 32 characters recommended)
SESSION_SECRET=your-random-secret-key-here-minimum-32-chars

# Environment - set to "production" on Digital Ocean
NODE_ENV=production

# Server port (Digital Ocean will set this automatically to 8080)
# PORT=8080

# ============================================================================
# FILE STORAGE (Optional)
# ============================================================================
# Local directory for file uploads (created automatically)
LOCAL_STORAGE_DIR=./storage

# ============================================================================
# OPTIONAL: GOOGLE INTEGRATIONS
# ============================================================================
# Only needed if setting up local Gmail/Google Sheets without Replit Connectors
# GMAIL_CLIENT_ID=your-client-id
# GMAIL_CLIENT_SECRET=your-client-secret
# GMAIL_REFRESH_TOKEN=your-refresh-token
# GOOGLE_CLIENT_ID=your-client-id
# GOOGLE_CLIENT_SECRET=your-client-secret
# GOOGLE_REFRESH_TOKEN=your-refresh-token
